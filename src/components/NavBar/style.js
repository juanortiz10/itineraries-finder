import { StyleSheet } from 'react-native';

import { WHITE, PRIMARY } from '../../consts';

export default StyleSheet.create({
  header: {
    marginTop: 24,
    backgroundColor: PRIMARY
  },
  headerTitle: {
    color: WHITE
  }
})
